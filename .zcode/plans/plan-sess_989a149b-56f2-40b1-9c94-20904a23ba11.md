**Mobile Financial Dashboard Tweaks – Implementation Plan**

**Goals**
Improve mobile usability: increase tap targets, fix visual consistency, ensure transaction order, keep Theme/Logout separate but better spaced.

**Files**
- client/styles/main.css
- client/js/dashboard.js

**CSS changes @media (max-width: 480px)**
- .tx-item padding 0.9rem 0.75rem, gap 0.75rem, min-height 56px, border-bottom separator
- .tx-desc 0.85rem, .tx-amount 0.9rem, .tx-meta 0.68rem
- .stat-card padding 0.9rem, gap 0.6rem, .stat-label 0.65rem, .stat-value 1.05rem
- .dashboard-card padding 0.9rem
- .bottom-nav-item padding 0.35rem 0.6rem, font-size 0.65rem, min-height 56px, icon size 1.15rem, justify-content space-evenly
- .bottom-nav visible padding 0.6rem 0 calc(0.4rem + env(safe-area-inset-bottom))
- .main-content padding-bottom calc(84px + env(safe-area-inset-bottom))
- .tx-amount.allocation color var(--status-positive)

**JS change**
In dashboard.js loadRecentTransactions, after fetching txs, sort client-side:
const sorted = [...txs].sort((a,b)=> new Date(b.transaction_date) - new Date(a.transaction_date));
Render sorted instead of txs.

Acceptance: newest-first transactions, allocation green, rows >=56px, nav tappable, content not hidden.
